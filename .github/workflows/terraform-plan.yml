name: terraform plan
on: push


jobs: 
  terraform_plan: 
    name: terraform plan
    runs-on: ubuntu-latest
    defaults: 
      run:
        shell: bash
    steps: 
    - name: checkout
      uses: actions/checkout@v4
    - name: terraform plan
      run: 
        terraform init
        terraform validate
        terraform plan  
